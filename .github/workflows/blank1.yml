name: My First Action

on:
  push:
    branches: [ main ]
  workflow_dispatch:  # Lets you trigger it manually

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: 23f2004968@ds.study.iitm.ac.in  # Your required step name
        run: echo "Hello from my GitHub Action! Email step complete."

